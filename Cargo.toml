[package]
name = "encrypted-tao"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
sqlx = { version = "0.5.7", features = ["postgres", "runtime-async-std-rustls"] }
dotenv = "0.15.0"
async-std = { version = "1.12.0", features = ["attributes"] }